# rhs-soccer-graphics

Public image host for Riverside Girls Soccer social posts.

Files here are served over `raw.githubusercontent.com` so Buffer, Facebook,
and X can fetch them when a post attaches a graphic. Nothing else lives in
this repo. Roster and player data stay in the private `soccer-social` repo.

Upload is automated; see `tools/post_graphic.py` in `soccer-social`.
